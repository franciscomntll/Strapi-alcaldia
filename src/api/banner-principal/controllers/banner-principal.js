'use strict';

/**
 *  banner-principal controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::banner-principal.banner-principal');
